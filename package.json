{
  "name": "@kutai.ai/common",
  "version": "1.0.6",
  "description": "This is the common library for kut-ai.",
  "main": "./build/index.js",
  "types": "./build/index.d.ts",
  "files": [
    "build/**/*"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rf ./build && npx tsc",
    "pub": "git add . && git commit -m \"Updates\" && npm version patch && npm run build && npm publish --access public"
  },
  "keywords": [
    "common",
    "kutai",
    "kut-ai",
    "kutai-common"
  ],
  "author": "Çağrı Kutay Ok",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "^4.17.18",
    "@types/jsonwebtoken": "^9.0.3",
    "@types/node": "^20.8.2",
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2"
  }
}
